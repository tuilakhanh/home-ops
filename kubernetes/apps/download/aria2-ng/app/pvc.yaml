---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: aria2-ng-config
  labels:
    app.kubernetes.io/name: aria2-ng
    app.kubernetes.io/instance: aria2-ng
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 512Mi
  storageClassName: openebs-hostpath
