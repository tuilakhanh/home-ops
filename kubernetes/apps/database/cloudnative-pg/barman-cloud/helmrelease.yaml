---
# yaml-language-server: $schema=https://flux.tholinka.dev/helmrelease/v2/github/cloudnative-pg/charts/main/charts/plugin-barman-cloud
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: barman-cloud
spec:
  chartRef:
    kind: OCIRepository
    name: plugin-barman-cloud
  interval: 1h
  values:
    crds:
      create: true
    rbac:
      create: true
